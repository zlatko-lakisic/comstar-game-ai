# Secrets and credentials
.cursor/secrets/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# IDE
.idea/
*.swp
*.swo

# Runtime / local config overrides
config/local.yaml
*.local.yaml
data/

# mTLS material fallback path
.ao-mtls/

# Runtime data
data/

# Jekyll / GitHub Pages
_site/
.jekyll-cache/
.sass-cache/
Gemfile.lock
vendor/

# Wiki knowledge base: raw export is a local build input, not a committed artefact.
# The text is CC BY-SA and the images are the publisher's assets under the wiki's own
# fair use claim; this repo is public. Only derived, reviewed output is committed.
kb/raw/
kb/cards/
